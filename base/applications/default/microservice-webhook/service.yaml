apiVersion: v1
kind: Service

metadata:
  name: microservice-webhook
  namespace: default

spec:
  type: ClusterIP
  ports:
    - name: metrics
      port: 8082
      protocol: TCP
      targetPort: metrics

  selector:
    app.kubernetes.io/name: microservice-webhook
    app.kubernetes.io/component: microservice